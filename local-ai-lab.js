(()=>{
 const KEY='vmusf_local_ai_lab';
 const state=()=>{try{return JSON.parse(localStorage.getItem(KEY)||'{}')}catch{return{}}};
 const save=x=>localStorage.setItem(KEY,JSON.stringify({...state(),...x}));
 function capabilities(){const mem=Number(navigator.deviceMemory||0),cores=Number(navigator.hardwareConcurrency||0),webgpu=!!navigator.gpu,wasm=typeof WebAssembly==='object';let tier='limited';if(webgpu&&(mem>=6||!mem)&&(cores>=4||!cores))tier='good';else if(wasm&&(mem>=4||!mem))tier='basic';return{webgpu,wasm,mem,cores,tier}}
 function render(){const c=capabilities(),s=state(),label=c.tier==='good'?'Good candidate':c.tier==='basic'?'May work with a small model':'Keep standard Assist';openModal('Local AI Lab',`<div class="result"><span class="tag">Experimental · device only</span><h3>Smarter Assist, without a per-message AI bill</h3><p>This lab checks whether your browser could run a small language model on this device. It does <b>not</b> download a model until you explicitly choose to.</p><div class="notice ${c.tier==='limited'?'warn':'good'}"><b>${label}</b><br>WebGPU: ${c.webgpu?'available':'not detected'} · WebAssembly: ${c.wasm?'available':'not detected'}${c.mem?` · Memory hint: ${c.mem} GB`:''}${c.cores?` · ${c.cores} logical cores`:''}</div><h3>What local AI would be allowed to do</h3><ul><li>Understand natural wording such as “my router blew up”.</li><li>Extract structured facts such as a missed callback or equipment model.</li><li>Suggest the most likely VMUSF journey.</li><li>Summarise information already in the member’s case.</li></ul><div class="notice"><b>What it will not decide</b><br>Ofcom rules, compensation, complaint deadlines and Virgin Media policy still come from VMUSF’s verified sources and workflows. AI is the language layer, not the authority.</div><label class="check"><input type="checkbox" id="localAiOpt" ${s.optedIn?'checked':''}> Remember that I want to try local AI on this device</label><div class="actions"><button class="btn primary" id="localAiSave">Save preference</button><button class="btn ghost" id="localAiTest">Run device check again</button></div><small>No model is bundled into the main VMUSF page, so normal visitors do not pay the performance cost.</small></div>`);document.getElementById('localAiSave').onclick=()=>{save({optedIn:document.getElementById('localAiOpt').checked,checkedAt:new Date().toISOString(),tier:c.tier});toastMsg('Local AI preference saved on this device.');closeModal()};document.getElementById('localAiTest').onclick=render}
 window.VMUSF_LOCAL_AI={capabilities,open:render,state};
 document.addEventListener('click',e=>{const b=e.target.closest('[data-local-ai-lab]');if(!b)return;e.preventDefault();render()});
})();