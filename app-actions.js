function go(r){if(moving||r===current)return;if(document.documentElement.dataset.motion==='reduced'){render(r);return}moving=true;trans.classList.add('go');setTimeout(()=>{render(r);setTimeout(()=>{trans.classList.remove('go');moving=false},210)},280)}
function openModal(title,html){modaltitle.textContent=title;modalbody.innerHTML=html;modalwrap.classList.add('open');document.body.style.overflow='hidden';setTimeout(()=>modalbody.querySelector('button,input,select,textarea')?.focus(),20);bindModal()}
function closeModal(){modalwrap.classList.remove('open');document.body.style.overflow='';document.getElementById('accbtn').focus({preventScroll:true})}document.getElementById('closemodal').onclick=closeModal;modalwrap.addEventListener('click',e=>{if(e.target===modalwrap)closeModal()});document.addEventListener('keydown',e=>{if(e.key==='Escape'){if(modalwrap.classList.contains('open'))closeModal();else access.classList.remove('open')}})
function toastMsg(s){const t=document.getElementById('toast');t.textContent=s;t.classList.add('show');clearTimeout(toastMsg.t);toastMsg.t=setTimeout(()=>t.classList.remove('show'),2200)}
function bindModal(){modalbody.querySelectorAll('[data-ma]').forEach(x=>x.onclick=()=>modalAction(x.dataset.ma,x.dataset))}
function action(a,d){if(a==='go-outages')return go('outages');if(a==='go-contracts')return go('contracts');if(a==='go-complaints')return go('complaints');if(a==='toast-community')return toastMsg('Your VMUSF community account stays with you.');if(a==='new-post')return openModal('Ask the community',`<form class="form" id="postform"><div class="field"><label for="pt">Topic title</label><input id="pt" required maxlength="90" placeholder="e.g. Hub 5 keeps dropping Wi-Fi"></div><div class="field"><label for="pc">Category</label><select id="pc"><option>Broadband</option><option>Wi-Fi</option><option>TV</option><option>Bills</option><option>Renewals</option><option>Complaints</option></select></div><div class="field"><label for="pb">What is happening?</label><textarea id="pb" required maxlength="900" placeholder="Include what you have already tried. Do not post account numbers, phone numbers or your full address."></textarea></div><div class="notice warn">Public forum reminder: remove personal account details before posting.</div><button class="btn primary" type="submit">Post question</button></form>`);if(a==='filter-posts')return toastMsg('Topic filters ready for the next backend pass.');if(a==='guided-help')return guidedStart();if(a==='official-help'||a==='complaint-guide')return openModal('Safe escalation checklist',`<div class="result"><h3>Keep it simple and evidence-based</h3><p class="lead">Record the date, what went wrong, what you asked the provider to do, what they replied, and whether the problem is resolved. Keep official complaint and escalation rules linked separately from community tips.</p><div class="notice good">VMUSF should help organise the facts, not invent legal rights or pretend a community answer is official.</div></div>`);if(a==='report-outage')return outageForm();if(a==='compare-deal')return compareForm();if(a==='edit-profile'||a==='switch-provider')return profileForm();if(a==='complaint-event')return complaintForm();if(a==='deal-detail')return openModal(d.deal||'Deal details',`<p class="lead">This is an illustrative comparison entry in the prototype. A production version should show verified source, date checked, contract length, setup fees, bundled services and total contract cost before ranking it.</p>`);if(a==='export-data')return exportData();if(a==='clear-data')return confirmClear()}
function guidedStart(){openModal('Guided help',`<div class="form"><p class="lead">What is the main problem?</p><div class="choices"><button class="choice" data-ma="gh-broadband">Broadband completely down</button><button class="choice" data-ma="gh-wifi">Wi-Fi keeps dropping</button><button class="choice" data-ma="gh-tv">TV service problem</button><button class="choice" data-ma="gh-bill">Bill or price problem</button></div></div>`)}
function modalAction(a){if(a==='gh-broadband')return openModal('Broadband check',`<div class="form"><div class="notice warn">Before changing settings, check whether other devices are also offline and whether there is a wider local report.</div><button class="btn primary" data-ma="gh-outage">Check community outage signal</button><button class="btn" data-ma="gh-cable">No, only my connection seems affected</button></div>`);if(a==='gh-outage'){closeModal();go('outages');return}if(a==='gh-cable')return openModal('Next safe step',`<div class="result"><h3>Check the physical basics first</h3><p class="lead">Confirm the hub has power, the coax connection is secure, and the problem affects more than one device. Avoid factory-resetting the hub as an early step.</p><div class="actions"><button class="btn" data-ma="gh-still">Still down</button><button class="btn primary" data-ma="gh-fixed">That fixed it</button></div></div>`);if(a==='gh-still')return openModal('Escalate the fault',`<div class="result"><h3>Time to check provider status or support</h3><p class="lead">You have ruled out the simplest local causes. Check provider service status and contact support if needed. If this becomes prolonged, start a timeline so you do not have to reconstruct it later.</p><button class="btn" data-ma="gh-complaint">Open complaint tracker</button></div>`);if(a==='gh-complaint'){closeModal();go('complaints');return}if(a==='gh-fixed'){closeModal();toastMsg('Great — you can mark this route as worked for you.');return}if(a==='gh-wifi')return openModal('Wi-Fi dropping',`<div class="result"><h3>Separate Wi-Fi from broadband</h3><p class="lead">If possible, check whether a wired device also loses internet at the same time. If wired stays online, focus on Wi-Fi coverage/interference rather than the broadband line.</p><div class="notice good">This distinction stops people changing broadband settings for a Wi-Fi-only problem.</div></div>`);if(a==='gh-tv')return openModal('TV problem',`<div class="result"><h3>Check whether broadband is affected too</h3><p class="lead">TV faults can be isolated or part of a wider service issue. Note the box model, exact on-screen message and whether other services work.</p></div>`);if(a==='gh-bill')return openModal('Bill or price problem',`<div class="result"><h3>Compare the numbers first</h3><p class="lead">Save the previous monthly price, new monthly price, date it changed and any package change. Then use the deal comparison page to avoid comparing unlike bundles.</p><button class="btn primary" data-ma="gh-deals">Open deal comparison</button></div>`);if(a==='gh-deals'){closeModal();go('deals')}}
function outageForm(){openModal('Report a service problem',`<form class="form" id="outageform"><div class="field"><label for="oa">Broad area</label><input id="oa" required maxlength="10" placeholder="e.g. S5"><small>Broad area only. Never your full postcode or address.</small></div><div class="field"><label for="os">Service</label><select id="os"><option>Broadband</option><option>Wi-Fi</option><option>TV</option><option>Phone</option></select></div><button class="btn primary" type="submit">Add community report</button></form>`)}
function compareForm(){openModal('Compare a renewal quote',`<form class="form" id="dealform"><div class="field"><label for="dq">Renewal quote per month</label><input id="dq" required type="number" min="0" step="0.01" value="61"></div><div class="field"><label for="dp">Public alternative per month</label><input id="dp" required type="number" min="0" step="0.01" value="39"></div><div class="field"><label for="dm">Contract months</label><input id="dm" required type="number" min="1" max="36" value="18"></div><button class="btn primary" type="submit">Compare prices</button></form>`)}
function profileForm(){const p=state.profile;openModal('Edit service snapshot',`<form class="form" id="profileform"><div class="field"><label>Provider</label><input id="provider" required value="${esc(p.provider)}"></div><div class="field"><label>Package</label><input id="package" required value="${esc(p.package)}"></div><div class="field"><label>Hub / router</label><input id="hub" value="${esc(p.hub)}"></div><div class="field"><label>Monthly price</label><input id="price" type="number" min="0" step="0.01" value="${Number(p.price)}"></div><div class="field"><label>Bill day</label><input id="billday" type="number" min="1" max="31" value="${Number(p.billDay)}"></div><div class="field"><label>Contract end</label><input id="contractend" type="date" value="${esc(p.contractEnd)}"></div><button class="btn primary" type="submit">Save snapshot</button></form>`)}
function complaintForm(){openModal('Add complaint update',`<form class="form" id="complaintform"><div class="field"><label>Update</label><select id="ct"><option>Provider replied</option><option>Engineer booked</option><option>Engineer attended</option><option>Missed appointment</option><option>Problem still unresolved</option><option>Problem resolved</option><option>Other update</option></select></div><div class="field"><label>Short note</label><textarea id="cn" maxlength="500" placeholder="What changed?"></textarea></div><button class="btn primary" type="submit">Add to timeline</button></form>`)}
function confirmClear(){openModal('Reset personal setup',`<div class="result"><h3>Reset this browser’s VMUSF prototype data?</h3><p class="lead">This removes the service snapshot, added posts, outage reports and complaint updates saved by this demo.</p><div class="actions"><button class="btn danger" data-ma="clear-yes">Reset data</button><button class="btn" data-ma="clear-no">Cancel</button></div></div>`);setTimeout(()=>{modalbody.querySelector('[data-ma="clear-yes"]').onclick=()=>{state=structuredClone(defaults);save();closeModal();render(current);toastMsg('Prototype data reset.')}})}
function exportData(){const blob=new Blob([JSON.stringify(state,null,2)],{type:'application/json'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='vmusf-my-data.json';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);toastMsg('Your VMUSF data file was created.')}
modalbody.addEventListener('submit',e=>{e.preventDefault();if(e.target.id==='postform'){state.posts.unshift({id:Date.now(),av:'YOU',title:pt.value.trim(),meta:`${pc.value} • just now`,text:pb.value.trim(),badge:'New question'});save();closeModal();render('community');toastMsg('Question added to this prototype.')}if(e.target.id==='outageform'){let area=oa.value.trim().toUpperCase(),service=os.value,x=state.outages.find(x=>x.area.toUpperCase()===area&&x.service===service);if(x)x.count++;else state.outages.unshift({area,service,count:1,status:'New report'});save();closeModal();render('outages');toastMsg('Community report added.')}if(e.target.id==='dealform'){let q=+dq.value,p=+dp.value,m=+dm.value,diff=q-p,total=diff*m;openModal('Price comparison',`<div class="result"><div class="compare"><div><small>Your current price</small><b>£${Number(state.profile.price).toFixed(2)}/mo</b></div><div><small>Renewal quote</small><b>£${q.toFixed(2)}/mo</b></div><div><small>Public alternative</small><b>£${p.toFixed(2)}/mo</b></div></div><div class="splitline"></div><div class="kpi">£${Math.abs(total).toFixed(2)}</div><p class="lead">${diff>0?`The renewal quote is £${diff.toFixed(2)} more per month than the public alternative, or £${total.toFixed(2)} across ${m} months.`:`The renewal quote is not more expensive than the alternative on monthly price alone.`}</p><div class="notice warn">Price alone is not enough. Check package contents, setup fees, contract length, speed, TV/phone services and any credits before deciding.</div></div>`)}if(e.target.id==='profileform'){state.profile={provider:provider.value.trim(),package:package.value.trim(),hub:hub.value.trim(),price:+price.value,billDay:+billday.value,contractEnd:contractend.value};save();closeModal();render(current);toastMsg('Service snapshot updated.')}if(e.target.id==='complaintform'){state.complaints.push({date:new Date().toLocaleDateString('en-GB',{day:'numeric',month:'short'}),title:ct.value,note:cn.value.trim()||'No extra note.'});save();closeModal();render('complaints');toastMsg('Complaint timeline updated.')}})
const access=document.getElementById('access'),ab=document.getElementById('accbtn');ab.onclick=()=>{const open=!access.classList.contains('open');access.classList.toggle('open',open);ab.setAttribute('aria-expanded',String(open))};function tog(id,key,attr,onv,offv){document.getElementById(id).onclick=()=>{const on=document.documentElement.dataset[attr]!==onv;document.documentElement.dataset[attr]=on?onv:offv;document.getElementById(id).textContent=on?'On':'Off';localStorage.setItem(key,on?onv:offv)}}tog('dark','vmusf-theme','theme','dark','light');tog('contrast','vmusf-contrast','contrast','high','normal');tog('motion','vmusf-motion','motion','reduced','full');let fs=+(localStorage.getItem('vmusf-text')||100);function text(){document.documentElement.style.setProperty('--fs',fs/100);localStorage.setItem('vmusf-text',fs)};document.getElementById('down').onclick=()=>{fs=Math.max(90,fs-10);text()};document.getElementById('reset').onclick=()=>{fs=100;text()};document.getElementById('up').onclick=()=>{fs=Math.min(140,fs+10);text()};['theme','contrast','motion'].forEach(a=>{const v=localStorage.getItem('vmusf-'+a);if(v)document.documentElement.dataset[a]=v});dark.textContent=document.documentElement.dataset.theme==='dark'?'On':'Off';contrast.textContent=document.documentElement.dataset.contrast==='high'?'On':'Off';motion.textContent=document.documentElement.dataset.motion==='reduced'?'On':'Off';if(!localStorage.getItem('vmusf-theme')&&matchMedia('(prefers-color-scheme:dark)').matches){document.documentElement.dataset.theme='dark';dark.textContent='On'}if(!localStorage.getItem('vmusf-motion')&&matchMedia('(prefers-reduced-motion:reduce)').matches){document.documentElement.dataset.motion='reduced';motion.textContent='On'}text();render(location.hash.slice(1)||'home');
