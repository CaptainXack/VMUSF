(()=>{
const root=document.documentElement,STORE='vmusf-access-plus-v1';let synth=window.speechSynthesis,utter=null,parts=[],partIndex=0,paused=false,activeEl=null;
const readPrefs=()=>{try{return JSON.parse(localStorage.getItem(STORE)||'{}')}catch{return{}}},savePrefs=p=>{try{localStorage.setItem(STORE,JSON.stringify(p))}catch{}};
function readableNodes(){return [...document.querySelectorAll('#app h1,#app h2,#app h3,#app .lead,#app p,#app li,#modalbody h2,#modalbody h3,#modalbody p,#modalbody li')].filter(el=>{const t=el.innerText?.trim();return t&&t.length>2&&el.offsetParent!==null&&!el.closest('nav,footer,.actions')})}
function build(){parts=readableNodes().map(el=>({el,text:el.innerText.replace(/\s+/g,' ').trim()}));partIndex=0}
function clearActive(){activeEl?.classList.remove('vm-read-active');activeEl=null}
function speakPart(){if(!synth||partIndex>=parts.length){stop();toastMsg?.('Finished reading.');return}clearActive();const p=parts[partIndex];activeEl=p.el;activeEl.classList.add('vm-read-active');if(root.dataset.readingFocus==='on')activeEl.scrollIntoView({behavior:root.dataset.motion==='reduced'?'auto':'smooth',block:'center'});utter=new SpeechSynthesisUtterance(p.text);utter.rate=1;utter.pitch=1;utter.onend=()=>{partIndex++;speakPart()};utter.onerror=e=>{if(e.error!=='canceled'&&e.error!=='interrupted'){partIndex++;speakPart()}};synth.speak(utter);updateBar()}
function start(){if(!('speechSynthesis'in window)){toastMsg?.('Read aloud is not supported by this browser.');return}stop(false);build();if(!parts.length){toastMsg?.('There is nothing useful to read on this screen yet.');return}document.getElementById('vm-readbar')?.classList.add('open');speakPart()}
function pauseResume(){if(!synth)return;if(synth.speaking&&!synth.paused){synth.pause();paused=true}else if(synth.paused){synth.resume();paused=false}updateBar()}
function stop(hide=true){try{synth?.cancel()}catch{}paused=false;utter=null;clearActive();if(hide)document.getElementById('vm-readbar')?.classList.remove('open');updateBar()}
function updateBar(){const b=document.getElementById('vm-read-toggle');if(b)b.textContent=paused?'▶':'Ⅱ';const l=document.getElementById('vm-read-status');if(l)l.textContent=parts.length?`Part ${Math.min(partIndex+1,parts.length)} of ${parts.length}`:'Ready'}
function setEasy(on){root.dataset.easyRead=on?'on':'off';const p=readPrefs();p.easy=on;savePrefs(p);sync()}
function setFocus(on){root.dataset.readingFocus=on?'on':'off';const p=readPrefs();p.focus=on;savePrefs(p);sync()}
function sync(){const easy=document.getElementById('easyread'),focus=document.getElementById('readfocus');if(easy)easy.textContent=root.dataset.easyRead==='on'?'On':'Off';if(focus)focus.textContent=root.dataset.readingFocus==='on'?'On':'Off'}
function enhance(){const panel=document.getElementById('access');if(panel&&!document.getElementById('readaloud')){const intro=document.createElement('p');intro.className='access-intro';intro.textContent='Choose how VMUSF looks, moves and speaks.';panel.querySelector('h3')?.after(intro);panel.insertAdjacentHTML('beforeend',`<div class="arow"><span>Read this page aloud</span><button id="readaloud">Play</button></div><div class="arow"><span>Reading focus</span><button id="readfocus">Off</button></div><div class="arow"><span>Easy Read</span><button id="easyread">Off</button></div>`);document.getElementById('readaloud').onclick=start;document.getElementById('readfocus').onclick=()=>setFocus(root.dataset.readingFocus!=='on');document.getElementById('easyread').onclick=()=>setEasy(root.dataset.easyRead!=='on')}
if(!document.getElementById('vm-readbar')){document.body.insertAdjacentHTML('beforeend',`<div class="readbar" id="vm-readbar" role="region" aria-label="Read aloud controls"><div class="readlabel"><b>VMUSF Read Aloud</b><small id="vm-read-status">Ready</small></div><button id="vm-read-toggle" class="primary" aria-label="Pause or resume">Ⅱ</button><button id="vm-read-stop" aria-label="Stop reading">■</button></div>`);document.getElementById('vm-read-toggle').onclick=pauseResume;document.getElementById('vm-read-stop').onclick=()=>stop()}
sync()}
const p=readPrefs();root.dataset.easyRead=p.easy?'on':'off';root.dataset.readingFocus=p.focus?'on':'off';enhance();
window.addEventListener('beforeunload',()=>stop(false));document.addEventListener('click',e=>{if(e.target.closest?.('[data-r],[data-a],[data-ma]'))setTimeout(()=>{if(synth?.speaking)stop();},40)},true);
window.VMUSF_ACCESS_PLUS={read:start,stop,setEasy,setFocus};
})();